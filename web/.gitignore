/node_modules
/.next/
/out/
/build
.DS_Store
*.pem
npm-debug.log*
# .env is committed on purpose for this dev deploy (see web/.env). Local overrides stay ignored.
.env*.local
.vercel
next-env.d.ts
/prisma/generated
# SPA build output — produced by `npm run build` (Vite builds frontend/ into here).
/public
