# DEV credentials committed intentionally so the Vercel deploy is self-contained.
# ⚠️ Rotate the DB password + JWT secret and move these to Vercel env vars before production.
# Supabase connection pooler (IPv4) — region ap-northeast-1 (Tokyo).
APP_DATABASE_URL="postgresql://postgres.vbbyztwglyzozoplmweh:F9YuH02j4eyXlCSD@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
APP_DIRECT_URL="postgresql://postgres.vbbyztwglyzozoplmweh:F9YuH02j4eyXlCSD@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres"
JWT_SECRET="r1sDenPmGuBdZTSRIrvd1YRKvLUr8DdeIF2ctpMXWbQ3sLe0OBhd7LVyBXOtwkcz"
# One-time admin bootstrap token (used by /api/setup). Remove this route + token after setup.
SETUP_TOKEN="vi-setup-7f3a9c"

# --- Supabase Storage (image uploads) ---
# Add the service_role key (Supabase → Settings → API) to store images in a public
# "media" bucket. Leave blank to keep images inline as data URLs.
SUPABASE_URL="https://vbbyztwglyzozoplmweh.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZiYnl6dHdnbHl6b3pvcGxtd2VoIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4MjQ3ODU4NSwiZXhwIjoyMDk4MDU0NTg1fQ.r3oKsxg04EXmO1-Z_X940-S9JRmZePJEHX0VN2BqjA4"
SUPABASE_STORAGE_BUCKET="media"

# --- Platform SMTP (OTP, password reset, and fallback sender) ---
# Fill these to make email go live everywhere. For Gmail: host smtp.gmail.com,
# port 587, user = your address, pass = a Gmail App Password.
SMTP_HOST="smtp.hostinger.com"
SMTP_PORT="465"
SMTP_USER="hello@viakashmiritinerary.in"
SMTP_PASS="Chingaar@2026"
SMTP_FROM="hello@viakashmiritinerary.in"
SMTP_FROM_NAME="ViaItinerary"

# --- Pixabay (destination photo search) ---
# Used server-side only by /api/destinations/photos so the key never reaches the browser.
PIXABAY_API_KEY="56316516-fbb10ad7475940758256bc517"

# --- Via Kashmir B2B rate portal (hotel catalog import) ---
# Public feed, no key needed. Override only for local/staging testing against
# a different deploy of https://github.com/viakashmir/b2bviakashmir.
B2B_VIAKASHMIR_URL="https://b2b.viakashmiritinerary.in"
# Protects /api/cron/sync-b2b-hotels — Vercel Cron sends this automatically as
# `Authorization: Bearer $CRON_SECRET` once the env var is set on the project;
# any other scheduler must send the same header. Leave unset to leave the
# route open (fine if nothing sensitive is exposed, but the header check is free).
CRON_SECRET="8341994f6f3e7afeb59a21969fb7c9d6bd4a92d84288a0ee"
